import { StyleSheet } from "react-native";

export const PropertyStyles = StyleSheet.create({
    bookmarkIcon:{
        left:'90%',
        zIndex: 1,
        color:'white'
    },
    cardImageStyle:{
        marginTop:'-10%',
        marginBottom:'-10%'
    }
});